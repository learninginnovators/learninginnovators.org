---
const content = [
  {
    type: "testimonial",
    feedback:
      "As a full-time professional, I needed flexible learning options. This platform exceeded my expectations. The self-paced courses and diverse subjects let me customize my learning journey. I've gained new skills that helped me stand out in my industry.",
    avatar: "/avatars/head2.svg",
    name: "Roberta Johnson",
    role: "Digital Marketing",
  },
  {
    type: "promotion",
    title: "A skill building journey with Enlightr",
    subtitle: "Read how Enlightr helped master news skills",
  },
  {
    type: "testimonial",
    feedback:
      "I'm amazed by the quality of courses offered on this platform. The instructors are experts in their fields, and the interactive learning approach keeps me engaged. I've gained skills I can immediately apply to my career",
    avatar: "/avatars/head1.svg",
    name: "Emilio Pirelli",
    role: "Data Science",
  },
  // Add more items as needed
];
---

<section>
  <div class="px-8 py-12 mx-auto md:px-12 lg:px-32 max-w-7xl lg:py-24">
    <ul
      class="grid grid-cols-1 text-center gap-6 mt-12 list-none md:grid-cols-2 lg:grid-cols-3 md:gap-12 md:mt-24"
      role="list">
      {
        content.map((item) => (
          <>
            {item.type === "testimonial" && (
              <li class="lg:order-first">
                <div class="h-full p-8 rounded-2xl lg:p-10 bg-blue-100">
                  <div class="flex flex-col items-center gap-4 h-full justify-between">
                    <p class="text-sm text-blue-900 font-display text-pretty">
                      {item.feedback}
                    </p>
                    <div class="flex flex-col gap-3 w-full items-center text-center justify-center order-first">
                      <img
                        class="inline-block w-12 h-12 rounded-full shrink-0"
                        src={item.avatar}
                        alt={item.feedback}
                      />
                      <div>
                        <p class="text-lg font-display text-blue-950">
                          {item.name}
                        </p>
                        <p class="text-blue-950 text-xs">{item.role}</p>
                      </div>
                    </div>
                  </div>
                </div>
              </li>
            )}
            {item.type === "promotion" && (
              <li class="items-center inline-flex order-first col-span-full lg:col-span-1">
                <div class="max-w-2xl mx-auto text-center">
                  <p class="text-4xl font-semibold font-display text-blue-950 lg:text-5xl">
                    {item.title}
                  </p>
                  <p class="text-slate-600 text-base mt-4  lg:text-xl text-balance">
                    {item.subtitle}
                  </p>
                </div>
              </li>
            )}
          </>
        ))
      }
    </ul>


  </div>
</section>
